@model IEnumerable<mvcNHS.DAL.GP_Services>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GP_Service_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GP_Service_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Service_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Service_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Service_ID })
        </td>
    </tr>
}

</table>
